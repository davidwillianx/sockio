{
  "name": "sockio",
  "version": "0.0.1",
  "description": "firt time using both (express/sockio) on node",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@davidwillianx",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.0"
  }
}
